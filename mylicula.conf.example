# MyLiCuLa Configuration File
#
# This is a blueprint/example configuration file. The actual configuration
# will be created at: ~/.config/mylicula/mylicula.conf
#
# DO NOT edit this file directly. Instead:
#   1. Run: ./install.sh
#   2. Or manually copy: cp mylicula.conf.example ~/.config/mylicula/mylicula.conf
#   3. Edit: ~/.config/mylicula/mylicula.conf with your actual values
#
# IMPORTANT: The actual config file may contain secrets (GitHub tokens, API keys, etc.)
#            and should NEVER be committed to version control.

# =============================================================================
# User Information
# =============================================================================

# Your system username (defaults to $USER if not specified)
CONFIG[USERNAME]="your_username"

# Your email address (used for git config, script headers, etc.)
CONFIG[EMAIL]="your.email@example.com"

# Your full name (used in script headers, documentation)
CONFIG[FULL_NAME]="Your Full Name"

# Company or organization name (leave as "Personal" if not applicable)
CONFIG[COMPANY]="Personal"

# Your GitHub username (used for cloning repos, etc.)
CONFIG[GITHUB_USER]="your_github_username"

# =============================================================================
# System Paths (usually auto-detected, override if needed)
# =============================================================================

# User home directory (auto-detected as $HOME)
CONFIG[HOME]="${HOME}"

# MyLiCuLa installation directory (auto-detected)
CONFIG[SCRIPT_DIR]="/path/to/mylicula"

# =============================================================================
# Secrets (Future Use)
# =============================================================================
# IMPORTANT: These will be added in future versions
#            DO NOT commit the actual config file with real tokens!

# GitHub Personal Access Token (for API access, private repos)
# Generate at: https://github.com/settings/tokens
# CONFIG[GITHUB_TOKEN]="ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# Other API keys as needed
# CONFIG[SOME_API_KEY]="your_api_key_here"

# =============================================================================
# Installation Options
# =============================================================================

# Ubuntu version (auto-detected)
CONFIG[UBUNTU_VERSION]="22.04"

# Whether running on Ubuntu (auto-detected: true/false)
CONFIG[IS_UBUNTU]="true"

# =============================================================================
# Notes
# =============================================================================
#
# Configuration Priority:
#   1. Values in ~/.config/mylicula/mylicula.conf (this file after setup)
#   2. Environment variables (MYLICULA_USERNAME, MYLICULA_EMAIL, etc.)
#   3. System defaults ($USER, git config, etc.)
#
# Reconfiguration:
#   To reconfigure, delete ~/.config/mylicula/mylicula.conf and run ./install.sh
#   Or manually edit ~/.config/mylicula/mylicula.conf
#
# Interpolation:
#   Values from this config replace <<<KEY>>> patterns in scripts during installation
#   Example: <<<USERNAME>>> becomes your actual username
#
